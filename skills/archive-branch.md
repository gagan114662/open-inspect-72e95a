# Skill: archive-branch

Load this before changing anything that touches: `archive`, `branch`, `commit`, `create`, `creation`.

## How this class of problem shows up

- Round 8: [P2] The selected comment was not bound to the triggering workflow_run's commit; an older review comment from a prior commit could be reused, or a comment from an unrelated concurrent run consumed.
- Round 8: [P2] Concurrency was scoped per-run-id, so overlapping runs could both pass the open-issue dedup check and create duplicate issues.
- Round 8: [P2] No processed-round state was persisted, so two separate PRs each contributing one finding on the same topic never combined into a real recurrence -- each was compared against the same static archive baseline in isolation, meaning cross-PR accumulation silently never happened despite the tool's 
- Round 9: **[P1]** Threshold detection still misses evidence accumulated through multiple pending archive PRs. In `.github/workflows/archive-and-recommend.yml`, each run compares the default-branch archive with only its own review. If a topic has one occurrence and two reviews each add another before either a
- Round 9: **[P2]** A failed PR creation leaves a branch that prevents recovery. The “Propose the archived round as a pull request” step pushes its deterministic branch before calling `gh pr create`, then suppresses creation errors. On retry, no open PR is found, so the workflow generates a new commit—includin
- Round 10: **[P2]** **Threshold crossings can be permanently missed.** Each run compares the default-branch archive plus its own round, excluding pending archive PRs. If two rounds together cross the threshold but neither does individually, both runs emit no recommendation. Merging their archive PRs does not t
- Round 10: **[P2]** **The concurrency configuration drops review rounds.** `cancel-in-progress: false` protects the running workflow, but the default queue allows only one pending run. If A is running and B is pending, arrival of C cancels B, leaving B’s review unarchived. Configure `queue: max` and provide re
- Round 10: **[P2]** **Archive PR creation cannot reliably recover after a partial failure.** If `git push` succeeds but `gh pr create` fails, the failure is swallowed and the remote branch remains. A rerun finds no open PR, creates a fresh commit on the same branch name, and ordinarily fails with a non-fast-fo

## Recurrence

Recurred in 31 round(s): 8, 9, 10, 14, 15, 16, 17, 18, 19, 20, 21, 22, 24, 25, 26, 29, 38, 39, 40, 43, 48, 54, 64, 65, 66, 67, 74, 89, 91, 98, 101. Mechanism-fix threshold: 3.

## How it was fixed

- Round 8: This is the round that attempts to close the specific gap named after round 6: 'I'm still the one deciding *when* to make a mechanism-level change versus a target-level fix.' scripts/detect-recurring-pattern.py already derived the recommendation from evidence; this round automates running it and acting on a crossing, bounded to the one action (opening a tracking issue) that is genuinely low-risk a

## Before you push

- Search your diff and its tests for the keywords above; each hit is a place a reviewer has flagged before.
- If the fix is a mechanism (a guard, a check, a workflow rule), add the regression test that fails without it.

_Generated by `scripts/distill-skills.py` from `docs/self-improvement-archive.jsonl`; do not edit by hand._
