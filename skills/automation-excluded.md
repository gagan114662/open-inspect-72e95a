# Skill: automation-excluded

Load this before changing anything that touches: `automation`, `excluded`, `github-bot`, `matching`, `pull-request-analytics-store`.

## How this class of problem shows up

- Round 11: **[P2]** `pull-request-analytics-store.ts`: The new query counts matching titles from every session source, although the response describes GitHub-bot reviews. A user or automation session titled `GitHub: Review PR #18` incorrectly increases both review totals. Add `spawn_source = 'github-bot'` and 
- Round 13: **[P2]** In `pull-request-analytics-store.ts`, the review query checks titles but never restricts `spawn_source`. A user or automation session titled `GitHub: Review PR #18` therefore increments the GitHub review metrics without being a bot review. Add `spawn_source = 'github-bot'` and test that mat

## Recurrence

Recurred in 2 round(s): 11, 13. Mechanism-fix threshold: 3.

## How it was fixed

- No round recorded a mechanism-level fix note for this topic yet. When you fix one, say how in the round's `mechanism_change_note` and this section fills in.

## Before you push

- Search your diff and its tests for the keywords above; each hit is a place a reviewer has flagged before.
- If the fix is a mechanism (a guard, a check, a workflow rule), add the regression test that fails without it.

_Generated by `scripts/distill-skills.py` from `docs/self-improvement-archive.jsonl`; do not edit by hand._
