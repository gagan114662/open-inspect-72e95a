# Skill: env-var-precedence

Load this before changing anything that touches: `precedence`, `env var`, `environment variable`, `unconditionally`.

## How this class of problem shows up

- Round 2: [P2] CODEX_API_KEY/OPENAI_API_KEY were exported unconditionally; Codex gives an API key precedence over stored ChatGPT auth when both are present, which could silently defeat subscription mode.
- Round 15: **[P2]** `scripts/improvement_policy.py:policy_hash` also sorts topic keys, giving differently ordered classifiers identical hashes. Reordering overlapping topics changes decisions but passes the stale-measurement guard. Hash the ordered topic sequence so the hash captures classification precedence.

## Recurrence

Recurred in 2 round(s): 2, 15. Mechanism-fix threshold: 3.

## How it was fixed

- No round recorded a mechanism-level fix note for this topic yet. When you fix one, say how in the round's `mechanism_change_note` and this section fills in.

## Before you push

- Search your diff and its tests for the keywords above; each hit is a place a reviewer has flagged before.
- If the fix is a mechanism (a guard, a check, a workflow rule), add the regression test that fails without it.

_Generated by `scripts/distill-skills.py` from `docs/self-improvement-archive.jsonl`; do not edit by hand._
