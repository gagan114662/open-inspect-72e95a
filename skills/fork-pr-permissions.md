# Skill: fork-pr-permissions

Load this before changing anything that touches: `fork`, `github_token`, `persist-credentials`.

## How this class of problem shows up

- Round 32: **[P2]** `.github/workflows/revise-improvement-policy.yml`, existing-PR lookup:
  `gh pr list --head "$branch"` matches the branch name without verifying its source repository. A
  fork PR named `improvement-policy-proposal` can therefore be selected. The workflow then posts its
  regeneration comment on

## Recurrence

Recurred in 1 round(s): 32. Mechanism-fix threshold: 3.

## How it was fixed

- No round recorded a mechanism-level fix note for this topic yet. When you fix one, say how in the
  round's `mechanism_change_note` and this section fills in.

## Before you push

- Search your diff and its tests for the keywords above; each hit is a place a reviewer has flagged
  before.
- If the fix is a mechanism (a guard, a check, a workflow rule), add the regression test that fails
  without it.

_Generated by `scripts/distill-skills.py` from `docs/self-improvement-archive.jsonl`; do not edit by
hand._
