# Skill: scripts-evidence

Load this before changing anything that touches: `scripts`, `evidence`, `topic`, `home`, `open-inspect`.

## How this class of problem shows up

- Round 12: **[P2]** `scripts/analyze-traces.py:114`: Searches cover the entire repository, but `--limit` only restricts hydration. Previously cached traces outside `trace_ids` can contribute matches, so `matching_traces` can exceed `traces_scanned` and results depend on cache history. Restrict matches to the s
- Round 12: **[P2]** `scripts/analyze-traces.py:101`: Each topic search requests only 100 results, then reports `len(matches)` as its count. Topics matching more than 100 traces are silently undercounted, potentially producing incorrect rankings. Paginate results or explicitly report truncation.
- Round 14: **[P2]** `scripts/analyze-traces.py`, `analyze()` — `--limit` controls which traces are listed and hydrated, but topic searches cover the entire directory. Previously cached traces outside that selection can contribute matches, so `matching_traces` can exceed `traces_scanned` and results depend on c
- Round 14: **[P2]** `scripts/analyze-traces.py`, `search_topic()` — Every topic search is capped at 100 results, and `analyze_by_topic()` reports the returned list length as the total. A topic matching more than 100 traces is silently undercounted, including with the default 200-trace scan. Paginate the matche
- Round 15: **[P2]** `scripts/measure-policy-validity.py:measure_epoch` calculates validity from unweighted recurrence, although the detector uses weighted recurrence and validity repair changes those weights. Consequently, discounting a topic cannot improve measured validity: identical evidence keeps triggerin
- Round 15: **[P2]** `scripts/measure-policy-validity.py:anchor_counts_at` treats topics missing from an evidence snapshot as having zero matching traces. After a revision adds topics, the workflow reuses the old snapshot, so unsearched topics can be classified as unsupported and have their weights discounted. 
- Round 16: **[P2]** `scripts/revise-improvement-policy.py`, `decide()`: Weight revisions can worsen validity and cannot trigger rollback. For recurrence `[2,1,2,1]`, anchor counts `[1,3,2,0]`, and weights `[0.5,1,1,1]`, restoring the first weight lowers validity from `0.2582` to `0`. The revision is accepted w
- Round 17: **[P2]** `scripts/revise-improvement-policy.py`, `decide()`: **Undefined candidate validity passes the acceptance rule.** Reweighting is rejected only when both scores are non-null. For recurrence `[2,1,1,1,1]` and anchor counts `[0,1,2,3,4]`, discounting the first topic changes validity from approx

## Recurrence

Recurred in 35 round(s): 12, 14, 15, 16, 17, 18, 19, 21, 22, 24, 26, 27, 28, 29, 30, 31, 33, 34, 35, 36, 37, 38, 41, 42, 45, 46, 47, 48, 55, 58, 59, 60, 61, 62, 64. Mechanism-fix threshold: 3.

## How it was fixed

- No round recorded a mechanism-level fix note for this topic yet. When you fix one, say how in the round's `mechanism_change_note` and this section fills in.

## Before you push

- Search your diff and its tests for the keywords above; each hit is a place a reviewer has flagged before.
- If the fix is a mechanism (a guard, a check, a workflow rule), add the regression test that fails without it.

_Generated by `scripts/distill-skills.py` from `docs/self-improvement-archive.jsonl`; do not edit by hand._
